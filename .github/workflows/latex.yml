name: Build Engineering Curves & Animations

on:
  push:
    branches:
      - main
    paths:
      - 'cycloid/**'
      - 'epicycloid/**'
      - 'hypocycloid/**'
      - 'animation/**'
      - '.github/workflows/latex.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      # --------------------------------------------------
      # Setup Python and Run Animation Generator
      # --------------------------------------------------
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Python Dependencies
        run: |
          pip install numpy matplotlib pillow

      - name: Generate Looping Cycloid Animation GIF
        run: |
          python animation/generate_cycloid.py

      # --------------------------------------------------
      # Compile LaTeX Presentations (PDFs)
      # --------------------------------------------------
      - name: Compile Cycloid Standard PDF
        uses: xu-cheng/latex-action@v4
        with:
          root_file: cycloid/cycloid.tex

      - name: Compile Cycloid Animated PDF
        uses: xu-cheng/latex-action@v4
        with:
          root_file: cycloid/cycloid_a.tex

      - name: Compile Epicycloid PDF
        uses: xu-cheng/latex-action@v4
        with:
          root_file: epicycloid/epicycloid.tex

      - name: Compile Hypocycloid PDF
        uses: xu-cheng/latex-action@v4
        with:
          root_file: hypocycloid/hypocycloid.tex

      # --------------------------------------------------
      # Commit and Push All Generated Assets Back to Repo
      # --------------------------------------------------
      - name: Save Generated Assets (PDFs and GIFs)
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add -f cycloid/cycloid.pdf
          git add -f cycloid/cycloid_a.pdf
          git add -f cycloid_animation.gif
          git add -f epicycloid/epicycloid.pdf
          git add -f hypocycloid/hypocycloid.pdf

          if git diff --cached --quiet; then
            echo "No changes detected."
          else
            git commit -m "build: update curve PDFs and generate looping cycloid animation gif"
            git push
          fi
